<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @collection.name %>
</p>

<p>
  <strong>Items:</strong>
  <ul>
    <% @collection.collectibles.each do |c| %>
		<li><%= c.name %></li>
	<% end %>
  </ul>
  <%=  %>
</p>

<%= link_to 'Edit', edit_collection_path(@collection) %> |
<%= link_to 'Back', collections_path %>
