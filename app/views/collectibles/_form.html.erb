<%= form_with(model: collectible, local: true) do |form| %>
  <% if collectible.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(collectible.errors.count, "error") %> prohibited this collectible from being saved:</h2>

      <ul>
      <% collectible.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :collection_id %>
    <%= form.select :collection_id, Collection.all.collect {|c| [ c.name, c.id ] }, { include_blank: true }%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
